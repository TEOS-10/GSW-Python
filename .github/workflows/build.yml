name: Make Release
# Just to test on the PR. then remove and add this
# on:
#   release:
#     types:
#       - published
on:
  pull_request:
  push:
    branches: [master]

jobs:
  packages:
    name: Build wheel on ${{ matrix.os }} and ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        python-version: [3.6, 3.7, 3.8]

    steps:
    - uses: actions/checkout@v2

    - name: Get tags
      run: git fetch --depth=1 origin +refs/tags/*:refs/tags/*

    - name: Install Python
      uses: actions/setup-python@v2

    - name: Install build tools
      run: |
        python -m pip install --upgrade pip
        python -m pip install --upgrade setuptools setuptools_scm wheel pep517 numpy twine

    - name: Build a binary wheel and a source tarball
      run: python -m pep517.build --source --binary . --out-dir dist
      
    - name: Check-REMOVEME
      run: |
        ls dist
# TODO set the token
    # - name: Publish Package
    #   uses: pypa/gh-action-pypi-publish@master
    #   with:
    #     user: __token__
    #     password: ${{ secrets.pypi_token }}